import { Roboto_Mono, Syne } from "next/font/google";

export const syne = Syne({
  subsets: ["latin"],
  display: "swap",
  weight: "700",
});

export const roboto = Roboto_Mono({
  subsets: ["latin"],
  display: "swap",
  weight: "500",
});
